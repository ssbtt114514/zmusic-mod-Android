pluginManagement {
    repositories {
        maven {
            name = 'NeoForge'
            url = 'https://maven.neoforged.net/releases/'
        }
        maven {
            name = 'AliYunGradle'
            url = 'https://maven.aliyun.com/repository/gradle-plugin'
        }
        maven {
            name = 'AliYunPublic'
            url = 'https://maven.aliyun.com/repository/public'
        }
        mavenCentral()
        gradlePluginPortal()
    }
}

plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = "amusic-neoforge"

include("amusic-core")
project(":amusic-core").projectDir = file("../../amusic-core")

def neoforgeProjects = [
        "amusic-neoforge-1.20.4",
        "amusic-neoforge-1.20.6",
        "amusic-neoforge-1.21.1",
        "amusic-neoforge-26.1.2",
        "amusic-neoforge-26.2"
]

def selectedProject = providers.gradleProperty("amusic.project").orNull
def selectedProjects = selectedProject ? [selectedProject] : neoforgeProjects

selectedProjects.each { projectName ->
    if (!neoforgeProjects.contains(projectName)) {
        throw new GradleException("Unknown NeoForge project: ${projectName}")
    }
    include(projectName)
    project(":${projectName}").projectDir = file("../../amusic-neoforge/${projectName}")
}
