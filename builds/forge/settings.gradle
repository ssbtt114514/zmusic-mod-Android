pluginManagement {
    repositories {
        maven {
            name = 'Forge'
            url = 'https://maven.minecraftforge.net/'
        }
        maven {
            name = 'AliYunGradle'
            url = 'https://maven.aliyun.com/repository/gradle-plugin'
        }
        maven {
            name = 'AliYunPublic'
            url = 'https://maven.aliyun.com/repository/public'
        }
        mavenCentral()
        gradlePluginPortal()
    }
}

plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = "amusic-forge"

include("amusic-core")
project(":amusic-core").projectDir = file("../../amusic-core")

def forgeProjects = [
        "amusic-forge-1.20.1",
        "amusic-forge-1.20.4"
]

def selectedProject = providers.gradleProperty("amusic.project").orNull
def selectedProjects = selectedProject ? [selectedProject] : forgeProjects

selectedProjects.each { projectName ->
    if (!forgeProjects.contains(projectName)) {
        throw new GradleException("Unknown Forge project: ${projectName}")
    }
    include(projectName)
    project(":${projectName}").projectDir = file("../../amusic-forge/${projectName}")
}
